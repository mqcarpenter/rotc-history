node_modules
.next
out
tsconfig.tsbuildinfo

.env
.env*.local
.DS_Store

# NOTE: data/league.db IS committed (not ignored) — Vercel builds from this
# checked-in snapshot instead of re-fetching from MFL on every deploy (MFL's
# API rate-limits hard, which made live-per-deploy imports unreliable). Run
# `npm run import` locally and commit the updated file whenever you want
# fresh data. See README.
